# Create git repository locally and on github
# Visit https://travis-ci.org/ and track your github repo
# Create the .travis.yml file

To deploy multi-container to Amazon Elastic Beanstalk (EB), EB is going to use the Amazon Elastic Container Service (ECS) which makes use of task definition to run Docker containers